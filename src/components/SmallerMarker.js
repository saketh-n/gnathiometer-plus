export default function SmallerMarker(props) {
  return (
    <div
      style={{
        width: "10px",
        height: "10px",
        backgroundColor: "red",
        borderRadius: "50%",
        cursor: "pointer",
        transform: "translate(-50%, -50%)",
      }}
    />
  );
}
